# Example: Angular refactor checklist

- Keep component inputs/outputs stable.
- Keep route paths and guards unchanged.
- Avoid template changes unless needed for readability.
- Prefer extracting pure functions for complex template logic.
- Ensure change detection strategy is unchanged unless explicitly planned.
- Run unit tests and lint checks.
